<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        ТРЕБОВАНИЕ ПРОГРАММЫ "ПРИЛОЖЕНИЯ ДЛЯ ВСЕЙ СЕМЬИ" (DESIGNED FOR FAMILIES)
        Явно удаляем разрешения на использование рекламных идентификаторов.
        Наличие этих разрешений (даже если они не используются) может привести к отклонению приложения
        при таргетинге на детскую аудиторию.
        НЕ УДАЛЯТЬ эти строки.
    -->
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupOnly="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locale_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">

        <!-- Отключаем автоматическое отслеживание экранов, так как используем ручное для Compose -->
        <meta-data
            android:name="google_analytics_automatic_screen_reporting_enabled"
            android:value="false" />

        <!--
            ТРЕБОВАНИЕ ПРОГРАММЫ "ПРИЛОЖЕНИЯ ДЛЯ ВСЕЙ СЕМЬИ" (DESIGNED FOR FAMILIES)
            Отключаем сбор рекламного идентификатора (AAID) на уровне конфигурации Firebase.
            Сбор AAID у детей запрещен политиками Google Play.
        -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />

        <!--
            ТРЕБОВАНИЕ ПРОГРАММЫ "ПРИЛОЖЕНИЯ ДЛЯ ВСЕЙ СЕМЬИ" (DESIGNED FOR FAMILIES)
            Отключаем персонализацию рекламы. Данные аналитики не должны использоваться для таргетинга.
        -->
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenLayout|screenSize"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
